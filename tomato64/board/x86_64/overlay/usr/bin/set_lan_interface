#!/bin/sh

set -e

if [ -z "$1" ]
then
	echo "Missing argument"
else
	for i in lan_ifnames landevs;
	do
		nvram set ${i}=$1
	done

	nvram set lan_hwaddr=`ifconfig $1 | grep -o -E ..:..:..:..:..:..`
fi
